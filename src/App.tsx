import * as React from 'react';
import * as ReactDOM from 'react-dom';

function render() {
    ReactDOM.render(<h2 className="text-red-500 font-bold">Hello from React and TailwindCSS!</h2>, document.body);
}

render();